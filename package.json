{
  "name": "www",
  "version": "1.1.0",
  "description": "A website for Raritan Public Library.",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "js-yaml": "^4.1.0",
    "pug": "^3.0.2",
    "stylus": "^0.56.0"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.0.1",
    "@parcel/transformer-image": "^2.0.1",
    "@parcel/transformer-pug": "^2.0.1",
    "@parcel/transformer-stylus": "^2.0.1",
    "parcel": "^2.0.1",
    "sharp": "^0.29.3"
  },
  "scripts": {
    "clear": "rm -rf dist && rm -rf .parcel-cache",
    "push": "node scripts/generate.js",
    "dev": "node scripts/generate.js && node scripts/htmlhack.js && parcel src/*.pug && node scripts/cleanup.js",
    "build": "npm run clear && node scripts/generate.js && parcel build src/*.pug && bash ./scripts/update.sh"
  }
}
