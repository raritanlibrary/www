{
  "name": "www",
  "version": "1.1.0",
  "description": "A website for Raritan Public Library.",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "js-yaml": "^4.1.0",
    "pug": "^3.0.2",
    "stylus": "^0.56.0"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.0.1",
    "@parcel/transformer-image": "^2.2.1",
    "@parcel/transformer-pug": "^2.2.1",
    "@parcel/transformer-stylus": "^2.2.1",
    "parcel": "^2.2.1",
    "sharp": "^0.29.3"
  },
  "scripts": {
    "clear": "rm -rf dist && mkdir dist && rm -rf .parcel-cache",
    "gen": "bash ./scripts/generate.sh",
    "pre-dev": "bash ./scripts/htmlhack.sh",
    "post-dev": "bash ./scripts/cleanup.sh",
    "push": "bash ./scripts/update.sh",
    "dev": "npm run gen && npm run pre-dev && parcel src/*.pug --open 'chrome' && npm run post-dev",
    "redev": "npm run clear && npm run dev",
    "build": "npm run clear && npm run gen && parcel build src/*.pug && npm run push"
  }
}
