{
  "name": "rpl",
  "version": "1.0.0",
  "description": "A website for Raritan Public Library.",
  "dependencies": {
    "fs-extra": "^9.1.0",
    "js-yaml": "^4.0.0",
    "pug": "^3.0.2",
    "stylus": "^0.54.8"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.0.0-beta.2",
    "@parcel/transformer-pug": "^2.0.0-beta.2",
    "@parcel/transformer-stylus": "^2.0.0-beta.2",
    "parcel": "^2.0.0-beta.2"
  },
  "scripts": {
    "dev": "parcel src/*.pug",
    "pre": "rm -rf dist && parcel build src/*.pug && node scripts/generate.js && netlify deploy",
    "build": "rm -rf dist && parcel build src/*.pug && node scripts/generate.js"
  },
  "author": "Spencer Gunning <swcgunning@gmail.com> (https://github.com/aemx)",
  "license": "MIT"
}
