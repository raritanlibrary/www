<!DOCTYPE html>
html(lang="en" color-mode="lite")
    head
        - const title = "Programs"
        - const desc = "Discover exclusive programs and upcoming events, only at Raritan Public Library."
        include ./includes/_head.pug
    body
        include ./includes/access.pug
        div.body
            include ./includes/header.pug
            main.main
                .sidebar-left.sidebar-craft
                    h3.hsidebar-event Adult Craft Projects
                    p.text.spacer-plus View our previous craft projects and see the great job done by our patron crafters!
                    -
                        const craftList = [
                            {name: "Winter Wreath", num: 2, img: "winter"},
                            {name: "Rag Candle Wreath", num: 3, img: "rag"},
                            {name: "Patriotic Birdhouse Wreath", num: 3, img: "birdhouse"},
                        ]
                    .crafts
                        for craft in craftList
                            h4 #{craft.name}
                            each img in arr(craft.num)
                                a.crafts-img(href=`url:./img/crafts/${craft.img}${img+1}.jpg` target="_blank")
                                    picture
                                        source.crafts-img-inner(srcset=`url:./img/crafts/_${craft.img}${img+1}.jpg?as=webp` type="image/webp")
                                        img.crafts-img-inner(src=`url:./img/crafts/_${craft.img}${img+1}.jpg?as=jpeg` alt=`${craft.name}` type="image/jpeg")
                .center
                    h2.h2-fo Programs
                    p.text.text-center To sign up for events, please use a provided sign-up form, or call us at #[b (908) 725-0413] during our open hours.
                    hr.hr
                    .programs
                .sidebar-right
                    include ./includes/contact.pug
                    include ./includes/hours.pug
                    include ./includes/advert.pug
            include ./includes/footer.pug