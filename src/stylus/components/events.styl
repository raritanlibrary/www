event-template(type)
    spacer(0.25)
    border 2px solid ev-alt(type)
    background-color ev(type)
    background-position 50% 5%
    background-blend-mode overlay
    user-select none
    transition all 0.2s ease-in-out
    &:hover, &:focus
        background-color ev-hover(type)
        border 2px solid type
    @media screen and (max-width 949px)
        spacer(0)
    &-cover
        background-color rgba(ev(type) 0.7)
        padding 8px
    &-title
        minion(21.2px, main-text(), bold)
        letter-spacing -0.1px
    &-subtitle
        perpetua(17px, main-text(), bold, italic)
        line-height 10px
    &-hr
        background main-text('aa')
        border 0
        margin 8px auto
        height 1px
        max-width 300px
        @media screen and (max-width 949px)
            margin 4px auto
            height 0px
    &-date
        perpetua(18px, ev-text(type), bold)
        @media screen and (max-width 949px)
            display none
        &-mobile
            display none
            @media screen and (max-width 949px)
                display block
                perpetua(16px, ev-text(type), bold)
    &-time
        perpetua(16px, ev-text(type), bold)
        margin-bottom 8px
        @media screen and (max-width 949px)
            display none
    &-desc
        perpetua(16px, ev-text(type))
        @media screen and (max-width 949px)
            display none
        &-filler
            display none
            @media screen and (max-width 949px)
                display block
                perpetua(16px, ev-text(type))
                margin-top 16px;
    text-align justify
    text-align-last center
    @media screen and (max-width 949px)
        width 215px
.calendar-events
    display flex
    flex-flow column wrap
    align-items center
    @media screen and (min-width 425px) and (max-width 949px)
        width calc(100% - 250px)
        flex-flow row nowrap
        justify-content flex-end
        gap 40px
.event
    @media screen and (min-width 750px) and (max-width 949px)
        &:nth-child(n+3)
            display none
    @media screen and (max-width 749px)
        &:nth-child(n+2)
            display none
    &-zoom
        display block
        spacer()
        border-radius 12px
        background-color event-zoom
        text-align center
        padding 6px 8px
        transition background-color 0.2s ease-in-out
        &:hover, &:focus
            background-color event-zoom-hover
        @media screen and (max-width 949px)
            spacer(0)
            padding 4px 8px
            margin-top 6px
        &-link
            lato(16px, alt-text, bold)
        svg
            position inline
            fill alt-text
            width 48px
            margin-left 2px
            margin-bottom -1px
    &-more
        display block
        visibility hidden
        border-radius 12px
        background-color alt()
        text-align center
        padding 6px 8px
        transition background-color 0.2s ease-in-out
        &:hover, &:focus
            background-color alt-lighten()
        @media screen and (max-width 949px)
            visibility visible
            padding 4px 8px
            margin-top 6px
        &-inner
            perpetua(18px, alt-text, bold)
    &-spacer
        spacer(-0.5)
        @media screen and (max-width 949px)
            spacer(0)
    &-red
        event-template(event-red)
    &-orange
        event-template(event-orange)
    &-yellow
        event-template(event-yellow)
    &-green
        event-template(event-green)
    &-blue, &-meeting
        event-template(event-blue)
    &-purple
        event-template(event-purple)
    &-pink, &-yoga
        event-template(event-pink)